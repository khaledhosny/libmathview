import json
import sys

f = open(sys.argv[1])
entities = json.load(f)['characters']

for entity in entities:
    value = "".join(["&#x%05X;" %(ord(c)) for c in entities[entity]])
    assert(value)
    sys.stdout.write('  <!ENTITY %s "%s">\n' %(entity, value))
