
* re-enable selection (need to adjust the wrapper method in MGD)
* re-enable actions
* implement reader interface over DOM and XmlReader
* add getId() method to reader interface
* factor the Element class
* factor ther View class
* implement table formatting
* implement line factory
* implement PS area model
* implement MGD for TeX fonts
* make debugging methods in Areas optional
* runtime selection of backend
* runtime selection of reader
* implement cursor
* implement the Area::find method more efficiently so that it
  doesn't visit parts of the area tree where the point cannot be

